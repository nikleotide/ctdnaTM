% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict.R
\name{dnaseq_dict_lookup}
\alias{dnaseq_dict_lookup}
\title{Look up raw vendor value(s) and return the canonical name(s)}
\usage{
dnaseq_dict_lookup(sub, value)
}
\arguments{
\item{sub}{One of `"variant_type"`, `"molecular_consequence"`,
`"cnv_type"`.}

\item{value}{Character vector of raw vendor strings. `NA` and empty
strings return `NA_character_`.}
}
\value{
Character vector of canonical names, same length as `value`.
  Unmapped inputs return `NA_character_`.
}
\description{
Handles combined `+`-separated values for `sub == "molecular_consequence"`
by splitting and returning the highest-priority component.
}
\examples{
# variant_type: real vendor strings resolve to canonical names.
# Unmapped strings return NA (per @return contract).
print(dnaseq_dict_lookup("variant_type",
  c("point_mutation", "INDEL", "CNA", "gene_fusion",
    "large_rearrangement", "banana")))
# -> c("SNV", "Indel", "CNV", "Fusion", "LGR", NA)

# molecular_consequence: combined "+" strings split; most-damaging wins.
print(dnaseq_dict_lookup("molecular_consequence",
  c("missense_variant",
    "frameshift_variant+splice_donor_variant+intron_variant",
    "STOP GAINED",
    "banana")))
# -> c("missense", "frameshift", "nonsense", NA)

# cnv_type: canonicalises amp / homozyg-del / LOH spellings.
print(dnaseq_dict_lookup("cnv_type",
  c("AMP", "GAIN", "hom_del", "LOH", "banana")))
# -> c("focal_amplification", "amplification",
#      "homozygous_deletion", "loh_deletion", NA)

# NA and empty strings pass through as NA.
print(dnaseq_dict_lookup("variant_type", c(NA, "", "SNV")))
}
\seealso{
\code{\link{dnaseq_dict}}, \code{\link{dnaseq_dict_add}},
  \code{\link{dnaseq_dict_reset}}.
}
