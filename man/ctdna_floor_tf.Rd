% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdnaTM.R
\name{ctdna_floor_tf}
\alias{ctdna_floor_tf}
\title{Pin tiny ctDNA values for log-scale display}
\usage{
ctdna_floor_tf(x, at = .o("display_floor"))
}
\arguments{
\item{x}{Numeric vector (ctDNA TF or MR values, in fraction units).}

\item{at}{Floor value (default \code{ctdna_opts("display_floor")}).}
}
\value{
Numeric vector the same length as x, with values \eqn{\geq} at.
}
\description{
Replaces any value smaller than \code{at} with \code{at}. Used before
a log scale so that undetectable / sub-floor measurements still appear
on the plot instead of becoming \code{-Inf}. Default is
\code{ctdna_opts("display_floor")} (0.001 by default).
}
\examples{
# Default: clamp to ctdna_opts("display_floor") (0.001)
ctdna_floor_tf(c(0, 1e-5, 0.001, 0.05, 0.5))

# Custom floor
ctdna_floor_tf(c(0, 0.0001, 0.01), at = 0.005)
}
