% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{ctdna_merge_modalities}
\alias{ctdna_merge_modalities}
\title{Merge per-subject ctDNA summary with another modality}
\usage{
ctdna_merge_modalities(ctdna_df, other_df, by = .o("subject"))
}
\arguments{
\item{ctdna_df}{Longitudinal ctDNA data (typically
\code{prep$samples}).}

\item{other_df}{Modality data with the subject column.}

\item{by}{Join key (default \code{ctdna_opts("subject")}, usually
\code{"Patient_ID"}).}
}
\value{
Merged data frame: one row per subject present in
  \strong{both} inputs. Column set is the union of
  \code{ctdna_summary(ctdna_df)} + \code{other_df} (minus the
  duplicate join key).
}
\description{
Convenience wrapper around [ctdna_summary()] + [merge()]. Collapses
the longitudinal ctDNA frame to one row per subject (via
\code{ctdna_summary}) and inner-joins that summary with another
per-subject modality frame keyed on the subject column.
}
\examples{
sim  <- ctdna_make_mock_study(n_patients = 20, seed = 1)
prep <- ctdna_prepare(
  infinity_report = sim$infinity_report,
  tf_change       = sim$tf_change,
  adam            = list(adsl = sim$clinical),
  verbose         = FALSE)
prep <- ctdna_sample_qc(prep, verbose = FALSE)

# Build a per-subject TMB frame from prep$samples and join it to the
# ctDNA summary.
tmb_df <- unique(prep$samples[, c("Patient_ID", "TMB_score")])
merged <- ctdna_merge_modalities(prep$samples, tmb_df)
print(head(merged))
}
\seealso{
\code{\link{ctdna_summary}} (the underlying per-subject
  collapse); \code{\link{ctdna_metric_at}} (extract a single
  per-subject metric at a chosen visit); the cross-modality plot
  family (\code{\link{ctdna_plot_vs_tmb}},
  \code{\link{ctdna_plot_vs_ihc}}, etc.).
}
