% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdnaTM.R
\name{ctdna_opts_io}
\alias{ctdna_opts_io}
\alias{ctdna_opts_save}
\alias{ctdna_opts_load}
\title{Save and load ctdna_opts() configuration}
\usage{
ctdna_opts_save(file)

ctdna_opts_load(file)
}
\arguments{
\item{file}{RDS file path.}
}
\value{
Invisibly, the file path (for save) or the loaded opts
  list (for load).
}
\description{
\code{ctdna_opts_save(file)} writes the current option registry to
an RDS file. \code{ctdna_opts_load(file)} restores it. Together
they let a study configuration be captured once, checked into a
repository, and reloaded in every downstream session without
having to reproduce a chain of \code{ctdna_opts(...)} setters by
hand.
}
\details{
Opts are session-global -- there is only one set at a time -- so
neither function takes a name argument. The saved payload is
tagged \code{"ctdnaTM_opts_payload"} so \code{ctdna_opts_load}
can refuse to consume a filter-scheme payload written by a
different save function.
}
\examples{
# Configure a couple of display options, save, reset, reload.
ctdna_opts(stat_position = "caption", legend_position = "bottom")
f <- tempfile(fileext = ".rds")
ctdna_opts_save(f)

ctdna_opts(.reset = TRUE)
print(ctdna_opts("stat_position"))    # default value

ctdna_opts_load(f)
print(ctdna_opts("stat_position"))    # "caption" -- restored

# Clean up.
ctdna_opts(.reset = TRUE)
unlink(f)
}
\seealso{
\code{\link{ctdna_opts}} (get / set individual options);
  \code{\link{ctdna_opts_choices}} (list keys with a restricted
  set of allowed values).
}
