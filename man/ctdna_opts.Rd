% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdnaTM.R
\name{ctdna_opts}
\alias{ctdna_opts}
\title{Get, set, print, reset, or bulk-load ctdnaTM options}
\usage{
ctdna_opts(..., load = NULL, domain = NULL, .reset = FALSE)
}
\arguments{
\item{...}{Unnamed character key(s) to get, or named values to set.}

\item{load}{Optional named list of options to apply in one call
(typically read back from \code{readRDS()}).}

\item{domain}{Optional character scalar restricting the printout to
one family of options. Accepts: \code{"clinical"} (subject /
cohort / recist / dose / mr / best_change / baseline /
cancertype), \code{"ctdna"} (per-sample ctDNA columns and the
methylation TF), \code{"genomic"} (gene + alteration columns),
\code{"tmb"} (TMB and MSI), \code{"ihc"} (marker + score),
\code{"expression"} (RNA-seq long-form), \code{"qc"} (sample-level
QC), \code{"filter"} (default filter scheme + indication
dictionary), \code{"rules"} (the \code{col_*} mappings used by the
rule engine), \code{"stats"}, \code{"plot"}, \code{"recist_values"}
(the CR/PR/SD/PD vocabulary), or \code{"*"} for the full dump.
Default \code{NULL} prints every key (and emits a tip about this
argument).}

\item{.reset}{If TRUE, reset to factory defaults.}
}
\value{
Invisibly, the current config (or the requested value).
}
\description{
Single entry point for configuration. Works like base R `options()`.
}
\details{
Modes (mutually exclusive):
\itemize{
  \item No args -> print and invisibly return current config
  \item One or more unnamed character keys -> get values
  \item Named arguments -> set values
  \item \code{.reset = TRUE} -> restore factory defaults
  \item \code{load = list(...)} -> bulk-load a saved config (replaces
        the older \code{do.call(ctdna_opts, cfg)} idiom)
}

\strong{Restricted keys.} Most \code{ctdna_opts()} keys accept any
value (column-name strings, palette colours, arbitrary tags), but a
subset is validated at set-time. Attempting to set one of these
keys to a disallowed value errors.

\emph{Restricted-value keys (must be one of the listed strings):}
\tabular{ll}{
  \code{pairwise_test}   \tab \code{"wilcox"} / \code{"t"} / \code{"kruskal"} / \code{"anova"} / \code{"auto"} \cr
  \code{cor_method}      \tab \code{"spearman"} / \code{"pearson"} / \code{"kendall"} \cr
  \code{stat_position}   \tab \code{"subtitle"} / \code{"caption"} / \code{"on_plot"} / \code{"none"} \cr
  \code{legend_position} \tab \code{"right"} / \code{"left"} / \code{"top"} / \code{"bottom"} / \code{"none"} \cr
  \code{expression_unit} \tab \code{"log2_tpm_plus_one"} / \code{"tpm"} \cr
}

\emph{Boolean keys (must be \code{TRUE} or \code{FALSE}):}
\code{show_stats}, \code{show_n}.

\emph{Numeric-range keys (must be a numeric scalar in the interval):}
\tabular{ll}{
  \code{display_floor} \tab \code{[1e-10, 1]} \cr
  \code{loq}           \tab \code{[1e-10, 1]} \cr
  \code{alpha}         \tab \code{[0, 1]} \cr
  \code{point_size}    \tab \code{[0.1, 20]} \cr
  \code{point_alpha}   \tab \code{[0, 1]} \cr
  \code{box_width}     \tab \code{[0.05, 1]} \cr
  \code{bar_width}     \tab \code{[0.05, 1]} \cr
  \code{discord_log10} \tab \code{[0, 10]} \cr
}

All other keys accept any value. Call
\code{\link{ctdna_opts_choices}()} for the same table dumped from
the runtime registry (useful at the console when you want a quick
reminder without opening the help page).
}
\examples{
\dontrun{
# Configure for a study and save the config
ctdna_opts(tf = "TF_methyl_v3", maxvaf = "MAX_VAF")
saveRDS(ctdna_opts(), "study123_config.rds")

# In a fresh session -- load the config in one call
cfg <- readRDS("study123_config.rds")
ctdna_opts(load = cfg)
}
}
