% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_cohen_kappa}
\alias{ctdna_cohen_kappa}
\title{Cohen's kappa from a contingency table}
\usage{
ctdna_cohen_kappa(tab)
}
\arguments{
\item{tab}{Square table or matrix. Row and column names should
name the same set of categories (extras are dropped).}
}
\value{
Numeric scalar: Cohen's kappa in \code{[-1, 1]}. Returns
  \code{NA_real_} when the table is empty or the expected agreement
  is exactly 1 (in which case kappa is undefined).
}
\description{
Unweighted Cohen's kappa on a square (or squarable) contingency
table whose row and column categories are meant to match. The
function trims to the intersection of \code{rownames(tab)} and
\code{colnames(tab)} before computing, so a nominal-agreement table
with different-but-overlapping labels is handled correctly.
}
\examples{
# Perfect agreement between two 3-level raters.
tab <- as.table(matrix(c(10, 0, 0,
                         0, 12, 0,
                         0, 0, 8), nrow = 3, byrow = TRUE,
                       dimnames = list(rater1 = c("PR","SD","PD"),
                                       rater2 = c("PR","SD","PD"))))
print(ctdna_cohen_kappa(tab))     # 1

# Some disagreement:
tab2 <- as.table(matrix(c(8, 2, 0,
                          1, 10, 1,
                          0, 1, 7), nrow = 3, byrow = TRUE,
                       dimnames = list(rater1 = c("PR","SD","PD"),
                                       rater2 = c("PR","SD","PD"))))
print(ctdna_cohen_kappa(tab2))
}
\seealso{
\code{\link{ctdna_weighted_kappa}} (ordinal / weighted
  variant); \code{\link{ctdna_concordance_test}} (bundled
  agreement summary).
}
