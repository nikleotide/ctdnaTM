% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integration.R
\name{ctdna_plot_vs_tmb}
\alias{ctdna_plot_vs_tmb}
\title{Plot ctDNA metric vs TMB from a chosen panel}
\usage{
ctdna_plot_vs_tmb(
  ctdna_df,
  tmb_df,
  panel = NULL,
  metric = .o("ctdna_metric"),
  tmb_col = .o("tmb"),
  recist_color = FALSE,
  scheme = c("three", "two", "four", "four_alt", "five", "six"),
  stat = c("spearman", "pearson", "kendall", "none"),
  show_stats = .o("show_stats"),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  xlab = NULL,
  ylab = NULL,
  point_size = .o("point_size") + 1,
  point_alpha = .o("point_alpha"),
  stat_position = .o("stat_position"),
  legend_position = .o("legend_position"),
  xlim = NULL,
  ylim = NULL,
  y_scale = "linear",
  indication = NULL,
  indications = NULL
)
}
\arguments{
\item{ctdna_df}{Longitudinal ctDNA data.}

\item{tmb_df}{Data with subject + TMB column (+ optional panel).}

\item{panel}{Optional panel filter.}

\item{metric}{ctDNA metric.}

\item{tmb_col}{Column name in `tmb_df` (default `ctdna_opts("tmb")`).}

\item{recist_color}{Logical. When \code{TRUE}, colour points by
RECIST (per \code{scheme}); the regression line is drawn in grey
underneath. \code{FALSE} (default) uses one grey colour for all
points.}

\item{scheme}{RECIST stratification scheme used when
\code{recist_color = TRUE}. One of \code{"three"} (default),
\code{"two"}, \code{"four"}, \code{"four_alt"}, \code{"five"},
\code{"six"}.}

\item{stat}{One of `"spearman"` (default), `"pearson"`, `"kendall"`,
`"none"`. Recommended: `"spearman"`.}

\item{show_stats, title, subtitle, caption, xlab, ylab, point_size, point_alpha}{Display.}

\item{stat_position}{Position for statistical annotation; passed
through to the stat layer. Default from
\code{ctdna_opts("stat_position")}. Scatter correlations report
in the caption, so this rarely needs changing.}

\item{legend_position}{ggplot legend position: \code{"right"}
(default from \code{ctdna_opts("legend_position")}),
\code{"bottom"}, \code{"top"}, \code{"left"}, or \code{"none"}.}

\item{xlim, ylim}{Optional numeric \code{c(lo, hi)} axis-limit pairs.
\code{NULL} (default) uses the data range. x-axis is always
\code{log10} (ctDNA metric); y-axis transform is controlled by
\code{y_scale}.}

\item{y_scale}{Y-axis transform for the TMB axis. \code{"linear"}
(default), \code{"log10"}, \code{"log2"}, or \code{"log"}.}

\item{indication}{Optional indication (cohort) restriction. A single string
or a character vector of Indication values, e.g.
\code{indication = "HNSCC"} or \code{indication = c("HNSCC","ES_SCLC")}.
\code{NULL} (the default) uses every indication present in the data.
Values are matched against the canonical \code{Indication} column,
resolved via \code{ctdna_opts("indication")} with fallbacks to
\code{Indication}, \code{indication}, \code{Cancertype} and
\code{Cancer_Type}. This is the modern spelling of the historical
\code{cancer_type=} argument.}

\item{indications}{Optional character vector of Indication values (e.g.
\code{c("NSCLC","mCRPC")}). When set, restricts the analysis to
patients whose Indication is in this set. Lookup falls through
Indication -> indication -> Cancertype -> Cancer_Type. \code{NULL}
(default) = no filter.}
}
\value{
A ggplot.
}
\description{
Scatter of a per-subject ctDNA metric against TMB from a long-format
TMB frame. ctDNA is plotted on a log10 x-axis (TMB on linear y by
default; `y_scale` accepts the usual transforms). A smoothed
linear-model fit with confidence band is overlaid. When
`show_stats = TRUE`, the chosen correlation (Spearman by default)
is computed on `log10(ctDNA)` vs `TMB` and reported in the caption
as `Spearman rho = ...; p = ...; N = ...`.
}
\section{Recommended statistic}{

`"spearman"` (default): TMB is right-skewed and ctDNA metrics span
orders of magnitude. Pearson is rarely appropriate.
}

\examples{
sim  <- ctdna_make_mock_study(n_patients = 20, seed = 1)
prep <- ctdna_prepare(
  infinity_report = sim$infinity_report,
  adam            = list(adsl = sim$clinical),
  verbose         = FALSE)
prep <- ctdna_sample_qc(prep, verbose = FALSE)

# TMB is carried on prep$samples (column TMB_score), so pass the
# samples frame for both the ctDNA and TMB inputs.
p1 <- ctdna_plot_vs_tmb(prep$samples, prep$samples)
print(p1)

# Colour points by RECIST scheme.
p2 <- ctdna_plot_vs_tmb(prep$samples, prep$samples,
  recist_color = TRUE, scheme = "three")
print(p2)
}
