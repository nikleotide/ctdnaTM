% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/platforms.R
\name{ctdna_qc_filter}
\alias{ctdna_qc_filter}
\title{Apply sample-level QC filtering to a prepared ctDNA bundle}
\usage{
ctdna_qc_filter(
  prep,
  sample_status = NULL,
  plasma_min = 0,
  dna_ng_min = 0,
  verbose = TRUE
)
}
\arguments{
\item{prep}{The output of \code{ctdna_prepare()}.}

\item{sample_status}{Character vector. Only samples whose
\code{Sample_status} value matches one of these (case-insensitive)
are kept. Default \code{NULL} = no Sample_status filter.}

\item{plasma_min}{Numeric. Minimum required \code{Plasma_ml_input}.
Samples below this are dropped. Default \code{0} = no filter.}

\item{dna_ng_min}{Numeric. Minimum required \code{cfDNA_ng}.
Samples below this are dropped. Default \code{0} = no filter.}

\item{verbose}{Logical. Print a summary of how many samples
passed. Default \code{TRUE}.}
}
\value{
A filtered version of \code{prep}. The \code{$samples}
  frame contains only passing samples; \code{$ctdna} is filtered
  to passing \code{(Patient_ID, Visit_name)} pairs; \code{$genomic_74}
  / \code{$genomic_500} are filtered to patients who have at least
  one passing sample.
}
\description{
Operates on the output of \code{\link{ctdna_prepare}}. Uses the
\code{$samples} frame (populated automatically by the GH Infinity
platform handler) to decide which samples pass the user-supplied
QC thresholds, then filters the longitudinal \code{$ctdna} and
baseline \code{$genomic_74} / \code{$genomic_500} frames so only
passing samples and their patients are retained.
}
\details{
\strong{v0.43.0}: \code{ctdna_prepare()} no longer applies any
sample-level filtering on its own; use this function explicitly
after \code{ctdna_prepare()} to apply QC.
}
\examples{
\dontrun{
prep <- ctdna_prepare(infinity_report = final_df0,
                       clinical        = pan_adrs)

# Keep PASS samples with >= 1 mL plasma input and >= 5 ng cfDNA:
prep <- ctdna_qc_filter(prep,
                         sample_status = c("PASS"),
                         plasma_min    = 1,
                         dna_ng_min    = 5)
}
}
