% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdnaTM.R
\name{ctdna_opts_choices}
\alias{ctdna_opts_choices}
\title{Show allowed values for restricted ctdna_opts keys}
\usage{
ctdna_opts_choices(key = NULL)
}
\arguments{
\item{key}{Optional key name. If omitted, prints the full table of
restricted keys and returns \code{NULL} invisibly. When supplied,
returns the allowed values as a character vector.}
}
\value{
Invisibly, the allowed values for \code{key}, or
  \code{NULL} when \code{key} is not restricted or is omitted.
}
\description{
Companion to \code{\link{ctdna_opts}}: prints (and invisibly returns)
the allowed values or numeric range for every restricted key. Use it
when you can't remember which strings a setting accepts.
}
\details{
\code{ctdna_opts_choices()} with no argument prints the full table.
\code{ctdna_opts_choices("stat_position")} returns the allowed
values for that one key as a character vector (or a numeric range
for numeric keys).
List the restricted-value keys in ctdna_opts()

A handful of \code{\link{ctdna_opts}} keys accept only a restricted
set of values (e.g. \code{stat_position} is one of
\code{"subtitle"}, \code{"caption"}, \code{"on_plot"},
\code{"none"}). This helper reports which keys are so restricted
and what their allowed values are.
}
\examples{
# Print the full table.
ctdna_opts_choices()

# Get the allowed values for one key.
print(ctdna_opts_choices("stat_position"))
}
\seealso{
\code{\link{ctdna_opts}} (get / set values);
  \code{\link{ctdna_opts_save}}, \code{\link{ctdna_opts_load}}
  (session-round-trip configs).
}
