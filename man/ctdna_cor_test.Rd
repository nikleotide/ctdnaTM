% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_cor_test}
\alias{ctdna_cor_test}
\title{Correlation test with rho, p, n}
\usage{
ctdna_cor_test(x, y, method = "spearman")
}
\arguments{
\item{x, y}{Numeric vectors.}

\item{method}{One of \code{"spearman"} (default), \code{"pearson"},
\code{"kendall"}. \code{"none"} short-circuits to a
no-computation return.}
}
\value{
A list with \code{method}, \code{name} (human-readable
  label), \code{rho} (the correlation estimate),
  \code{p} (two-sided p-value), \code{n} (non-\code{NA} pair
  count). Non-computable inputs (\code{n < 3} or \code{method =
  "none"}) return \code{rho = NA} and \code{p = NA}.
}
\description{
Runs Spearman (default), Pearson, or Kendall correlation on two
numeric vectors. Missing values are excluded pairwise. Wraps
\code{stats::cor.test} with \code{exact = FALSE} and warnings
suppressed, so tied ranks and small samples do not error.
}
\examples{
set.seed(1)
x <- runif(30)
y <- 2 * x + rnorm(30, sd = 0.3)
print(ctdna_cor_test(x, y))                       # Spearman (default)
print(ctdna_cor_test(x, y, method = "pearson"))
print(ctdna_cor_test(x, y, method = "kendall"))
}
\seealso{
\code{\link{ctdna_paired_test}},
  \code{\link{ctdna_run_group_test}},
  \code{\link{ctdna_pval_label}}.
}
