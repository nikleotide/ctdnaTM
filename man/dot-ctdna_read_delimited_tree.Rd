% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/platforms.R
\name{.ctdna_read_delimited_tree}
\alias{.ctdna_read_delimited_tree}
\title{Read a directory tree of per-sample delimited files into one long frame}
\usage{
.ctdna_read_delimited_tree(
  loc,
  regex,
  modality = c("dnaseq", "rnaseq"),
  verbose = TRUE
)
}
\arguments{
\item{loc}{Directory root (character path).}

\item{regex}{Glob pattern relative to `loc`. Example:
"*/variant/annotated/*.clinical.annotated.tsv"
- Dots are literal
- `*` matches any character except path separator
- The FIRST `*` in the FILENAME (last path segment) becomes Sample_ID}

\item{modality}{"dnaseq" (default) or "rnaseq" -- selects which opts keys
drive the column canonicalisation.}

\item{verbose}{Show a progress bar and per-file status. Default `TRUE`.}
}
\value{
A data.frame with (at minimum) Sample_ID, Patient_ID, Gene,
  Variant_type. Additional canonical columns are added per opts settings.
  Rows failing the QC filter (if `dnaseq_qc_filter_col` exists) or below
  `dnaseq_min_vaf` are dropped.
}
\description{
Walks `loc` looking for files matching `regex` (glob pattern), reads each,
extracts Sample_ID from the filename wildcard, canonicalises columns via
ctdna_opts() keys, and concatenates. Used internally by ctdna_prepare()
when the user supplies `dnaseq = list(loc = ..., regex = ...)` or the
equivalent for rnaseq.
}
\keyword{internal}
