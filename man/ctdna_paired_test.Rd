% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_paired_test}
\alias{ctdna_paired_test}
\title{Paired test (baseline vs each timepoint)}
\usage{
ctdna_paired_test(x, y, method = "paired_wilcox")
}
\arguments{
\item{x, y}{Numeric vectors of equal length (paired observations).}

\item{method}{One of \code{"paired_wilcox"} (default),
\code{"paired_t"}, \code{"wilcox"}, \code{"t"}. \code{"none"}
short-circuits.}
}
\value{
A list with \code{method} (as supplied), \code{name}
  (human-readable label), \code{p} (two-sided p-value), \code{n}
  (non-\code{NA} pair count). Returns \code{p = NA} when the
  underlying test errors or \code{n < 2}.
}
\description{
Paired or unpaired Wilcoxon signed-rank / t-test for two equal-length
numeric vectors of paired observations. \code{"paired_wilcox"}
(default) and \code{"paired_t"} treat elements pairwise; \code{"wilcox"}
and \code{"t"} drop the pairing (used when the caller wants the
same catalog string across paired and unpaired plots).
}
\examples{
# Simulate baseline / on-treatment paired measurements.
set.seed(1)
baseline <- runif(20, min = 5, max = 15)
post     <- baseline * runif(20, min = 0.2, max = 0.9)  # decreases
print(ctdna_paired_test(baseline, post))                # paired Wilcoxon
print(ctdna_paired_test(baseline, post, method = "paired_t"))

# Unpaired variant on the same data:
print(ctdna_paired_test(baseline, post, method = "wilcox"))
}
\seealso{
\code{\link{ctdna_run_group_test}} (independent groups);
  \code{\link{ctdna_cor_test}}; \code{\link{ctdna_pval_label}}.
}
