% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_concordance_test}
\alias{ctdna_concordance_test}
\title{Concordance summary for a 2-way table}
\usage{
ctdna_concordance_test(tab, method = "both")
}
\arguments{
\item{tab}{Contingency table (rows and columns naming the same
category set).}

\item{method}{One of \code{"both"} (default; kappa + agreement +
McNemar when applicable), \code{"kappa"} (only Cohen's kappa),
\code{"agreement"} (only proportion agreement),
\code{"mcnemar"} (only McNemar; requires 2x2), or
\code{"none"} (short-circuit).}
}
\value{
A list with numeric components. \code{$method} echoes the
  argument; \code{$agreement} is the proportion of on-diagonal
  subjects; \code{$kappa} is unweighted Cohen's kappa; \code{$mcnemar_p}
  is the McNemar two-sided p (\code{NA_real_} for non-2x2 or when
  \code{method} excludes it); \code{$n} is the table total.
}
\description{
Bundles the three concordance measures most commonly reported on a
rater-agreement or modality-agreement contingency table: raw
percent agreement, Cohen's kappa, and (for 2x2 tables only) the
McNemar test for marginal-homogeneity of paired binary outcomes.
The n reported is the table total.
}
\examples{
# 3-level (non-2x2): kappa + agreement, no McNemar.
tab <- as.table(matrix(c(8, 2, 0,
                         1, 10, 1,
                         0, 1, 7), nrow = 3, byrow = TRUE,
                       dimnames = list(rater1 = c("PR","SD","PD"),
                                       rater2 = c("PR","SD","PD"))))
print(ctdna_concordance_test(tab))

# 2x2: McNemar is included.
tab2 <- as.table(matrix(c(30, 5,
                           8, 27), nrow = 2, byrow = TRUE,
                         dimnames = list(assay1 = c("pos","neg"),
                                         assay2 = c("pos","neg"))))
print(ctdna_concordance_test(tab2))
}
\seealso{
\code{\link{ctdna_cohen_kappa}},
  \code{\link{ctdna_weighted_kappa}} (ordinal variant);
  \code{\link{ctdna_run_group_test}} (independent-groups
  comparisons).
}
