% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_plots.R
\name{ctdna_alteration_grid}
\alias{ctdna_alteration_grid}
\title{Combinatorial alteration stacked-bar grid}
\usage{
ctdna_alteration_grid(
  prep,
  gene_sets,
  wrap = NULL,
  response_col = "RECIST",
  response_levels = NULL,
  scheme = c("raw", "two", "three", "four"),
  scales = c("fixed", "free_y", "free_x", "free"),
  filter_scheme = NULL,
  visit = "C1D1",
  visit_col = "Visit_name",
  patient_col = "Patient_ID",
  gene_col = "Gene",
  stat = c("fisher", "chisq", "none"),
  show_counts = TRUE,
  title = "Combinatorial alteration status",
  subtitle = NULL,
  caption = NULL,
  indications = NULL
)
}
\arguments{
\item{prep}{A `ctdna_prep` object.}

\item{gene_sets}{A named list where each entry is either a bare gene-symbol
vector (= `alt_any`, backward-compatible) OR a wrapped set from
[alt_any()], [alt_all()], or [alt_any_n()]. The wrapper carries the
combine mode for that set. Facet columns appear in the order the sets
are listed. Unnamed entries get auto-labelled.}

\item{wrap}{Optional column in `patient_data` used as the row-facet
variable (e.g. `"Indication"`, `"Dose"`). NULL (default) = no row facet.}

\item{response_col}{Column with the response category (default `"RECIST"`).}

\item{response_levels}{Optional level order for the x-axis.}

\item{scheme}{RECIST collapsing scheme: `"raw"` (default), `"two"`,
`"three"`, or `"four"`.}

\item{scales}{Facet `scales=` argument: `"fixed"`, `"free_y"`, etc.}

\item{filter_scheme}{Optional filter scheme(s) applied to `prep$variants`.}

\item{visit}{Visit to restrict to (default `"C1D1"`). Accepts `"Cycle 1
Day 1"` and `"baseline"` (case-insensitive).}

\item{visit_col}{Column in `prep$variants` carrying the visit.}

\item{patient_col, gene_col}{Column overrides.}

\item{stat}{`"fisher"` (default), `"chisq"`, or `"none"`.}

\item{show_counts}{If TRUE, prints `pct% (n)` inside each segment.}

\item{title, subtitle, caption}{Plot labels.}

\item{indications}{Optional character vector of Indication values (e.g.
\code{c("NSCLC","mCRPC")}). When set, restricts the analysis to
patients whose Indication is in this set. Lookup falls through
Indication -> indication -> Cancertype -> Cancer_Type. \code{NULL}
(default) = no filter.}
}
\value{
`list(plot, summary, stats)`.
}
\description{
Stacked-bar grid showing the proportion of patients with an alteration in
each of several gene sets, broken down by response category, optionally
faceted by another column (default: no row facet -- one row of bars across
gene sets). Same patient base as [ctdna_oncoprint()].
}
