% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdnaTM.R
\name{ctdna_summary}
\alias{ctdna_summary}
\title{Per-subject ctDNA summary for multi-modal integration}
\usage{
ctdna_summary(df)
}
\arguments{
\item{df}{A longitudinal ctDNA data frame -- typically
\code{prep$samples} from \code{\link{ctdna_prepare}} after
\code{\link{ctdna_sample_qc}}, but any frame carrying the
canonical subject / visit / TF columns works.}
}
\value{
A data frame with one row per subject; column order is
  subject-ID first, followed by \code{baseline_tf},
  \code{best_ratio}, \code{last_tf}, \code{best_lesion_change}
  (when derivable), and any preserved per-patient metadata columns.
}
\description{
Collapses a longitudinal per-sample ctDNA frame to one row per
subject with the canonical summary metrics (\code{baseline_tf},
\code{best_ratio}, \code{last_tf}) and any per-patient metadata
present on the input (RECIST, Dose, Cohort, MR, best change).
Serves as the join key between ctDNA and orthogonal modalities in
the cross-modality plots (\code{\link{ctdna_plot_vs_tmb}},
\code{\link{ctdna_plot_vs_ihc}}, etc.).
}
\details{
Reads the per-subject / per-visit / per-TF-column trio via the
current \code{ctdna_opts()} configuration (defaults:
\code{Patient_ID}, \code{Visit_name},
\code{Methylation_tumor_fraction_percentage}), then derives:
\itemize{
  \item \code{baseline_tf} -- TF at the baseline visit
    (\code{ctdna_opts("baseline")}, typically \code{"C1D1"}).
  \item \code{best_ratio} -- minimum on-treatment / baseline TF
    ratio per subject (deepest molecular response).
  \item \code{last_tf} -- TF at the chronologically last
    on-treatment visit.
  \item \code{best_lesion_change} -- the best (most-negative)
    RECIST target-lesion percent change, derived from the source
    column with fall-through preference:
    \code{best_lesion_change} if already broadcast onto the frame,
    else \code{ctdna_opts("lesion_pct")} (typically ADTR
    \code{PCHG}), else computed from the per-visit lesion-size
    column \code{ctdna_opts("lesion")}.
  \item Any per-patient metadata columns already present:
    \code{RECIST}, \code{Dose}, \code{cohort}, \code{MR},
    \code{best_change}.
}

\code{Record_type == "tumor"} rows (imaging-derived rows sometimes
interleaved on the samples frame) are dropped before the summary
is computed.
}
\examples{
sim  <- ctdna_make_mock_study(n_patients = 20, seed = 1)
prep <- ctdna_prepare(
  infinity_report = sim$infinity_report,
  adam            = list(adsl = sim$clinical),
  verbose         = FALSE)
prep <- ctdna_sample_qc(prep, verbose = FALSE)

# One row per patient with baseline_tf / best_ratio / last_tf +
# preserved metadata columns (RECIST, Dose, etc.).
summ <- ctdna_summary(prep$samples)
print(head(summ))
}
\seealso{
\code{\link{ctdna_metric_at}} (more flexible per-subject
  extraction at a chosen visit and metric);
  \code{\link{ctdna_merge_modalities}} (join the summary with
  another modality by subject);
  \code{\link{ctdna_prepare}}, \code{\link{ctdna_sample_qc}}
  (upstream requirements).
}
