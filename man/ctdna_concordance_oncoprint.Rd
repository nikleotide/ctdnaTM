% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concordance_oncoprint.R
\name{ctdna_concordance_oncoprint}
\alias{ctdna_concordance_oncoprint}
\title{Concordance oncoprint (ctDNA vs tissue)}
\usage{
ctdna_concordance_oncoprint(
  prep,
  tissue_df,
  gene_sets = NULL,
  filter_scheme = NULL,
  visit = "baseline",
  wrap = NULL,
  indications = NULL,
  patient_data = NULL,
  annotations = NULL,
  show_freq_bar = TRUE,
  alterations = NULL,
  sort_genes = c("global", "within_set", "none"),
  max_patients_per_panel = 25,
  show_patient_names = NA,
  sort_by = c("combined_burden", "ctdna_burden", "tissue_burden", "patient"),
  title = "ctDNA vs tissue concordance",
  subtitle = NULL,
  caption = NULL,
  legend_position = "right"
)
}
\arguments{
\item{prep}{A \code{ctdna_prep} object from \code{ctdna_prepare()}.}

\item{tissue_df}{data.frame with at least Patient_ID, Gene, Variant_type
(e.g. \code{prep$dnaseq} or a WES call frame). Optionally an
\code{Alteration_class} column overrides the built-in classifier.}

\item{gene_sets}{Named list of gene vectors. \code{NULL} (default) uses
the union of every gene called in either data source. Set names are
used by \code{sort_genes = "within_set"}.}

\item{filter_scheme}{Optional variant-filter scheme(s) applied to
\code{prep$variants} before analysis.}

\item{visit}{Visit filter for ctDNA. \code{"baseline"} (default)
expands to \code{c("C1D1","Cycle 1 Day 1","Baseline","Screening")}.
Use \code{"all"} or a specific visit name to override.}

\item{wrap}{Optional column in \code{prep$clinical} to facet by
(e.g. \code{"Indication"}).}

\item{indications}{Optional character vector of Indication values to
restrict the cohort to. \code{NULL} (default) = no filter.}

\item{patient_data}{Optional per-patient annotation data.frame keyed by
Patient_ID. Overrides \code{annotations} if both are supplied.}

\item{annotations}{Character vector of column names in \code{prep$clinical}
to render as annotation strips beside each patient. Each column gets
its own legend (requires the \code{ggnewscale} package).}

\item{show_freq_bar}{Show the per-gene prevalence bar at the top
(dodged bars, ctDNA vs Tissue). Default \code{TRUE}.}

\item{alterations}{Optional character vector restricting which alteration
classes are drawn. Must be a subset of Focal_Amp / Amp /
Homozygous_Del / LOH / Missense / Truncating / InFrame / Promoter /
LGR / Fusion / Other.}

\item{sort_genes}{Gene column ordering. \code{"global"} (default) sorts
by combined prevalence; \code{"within_set"} sorts within each
\code{gene_sets} group; \code{"none"} keeps input order.}

\item{max_patients_per_panel}{Cap patients per facet by top-N combined
burden. Default \code{25}.}

\item{show_patient_names}{\code{TRUE} / \code{FALSE} / \code{NA} (auto).}

\item{sort_by}{Patient row ordering within each facet.}

\item{title, subtitle, caption}{Plot chrome.}

\item{legend_position}{Passed to \code{theme(legend.position = ...)}.}
}
\value{
A composed \code{ggplot} (via patchwork) with class
  \code{"ctdna_concordance_oncoprint"}. Add layers directly with
  \code{+}. Auxiliary data attached as attributes: \code{ctdna_matrix},
  \code{tissue_matrix}, \code{plot_dim}, \code{patients}, \code{genes},
  \code{long}, \code{n_dropped_by_topN}.
}
\description{
Transposed oncoprint pairing each patient's ctDNA calls with their
orthogonal tissue-genomic calls. Rows: patients, two per patient
(ctDNA on top, tissue underneath). Columns: genes. Cells: alteration
glyph with the same palette and thin-bar / full-cell rules as
\code{\link{ctdna_oncoprint}}.
}
\details{
Full annotation surface: \code{annotations} (or \code{patient_data})
render clinical strips at the left; \code{show_freq_bar} adds a per-gene
prevalence bar at the top; \code{alterations} filters alteration types;
\code{indications} restricts the cohort.
}
\examples{
\dontrun{
sim <- ctdna_make_mock_study(n_patients = 60, seed = 3)
prep <- ctdna_prepare(infinity_report = sim$infinity_report,
                       adam = list(adsl = sim$clinical),
                       verbose = FALSE)
res <- ctdna_concordance_oncoprint(
  prep,
  tissue_df   = sim$dnaseq,
  gene_sets   = list(TSG = c("TP53","RB1","PTEN")),
  visit       = "baseline",
  wrap        = "Indication",
  indications = c("mCRPC","NSCLC"),
  annotations = c("Dose","RECIST"))
res + ggplot2::theme(axis.text = ggplot2::element_text(size = 6))
}

}
