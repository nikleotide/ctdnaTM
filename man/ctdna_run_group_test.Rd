% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_run_group_test}
\alias{ctdna_run_group_test}
\title{Run a single group-comparison test}
\usage{
ctdna_run_group_test(data, value, group, method = "auto")
}
\arguments{
\item{data}{Data frame.}

\item{value}{Numeric column name.}

\item{group}{Grouping column name.}

\item{method}{One of \code{"auto","wilcox","t","kruskal","anova","none"}.}
}
\value{
A list with elements \code{method} (the test actually run,
  after \code{"auto"} is resolved), \code{name} (human-readable test
  label used in plot captions), \code{p} (numeric p-value or
  \code{NA} if the test could not be run), and \code{n} (non-missing
  observation count).
}
\description{
Selects a two-group or multi-group test appropriate for the data
and runs it. Used inside plot functions to compute the p-value that
captions and legends display. \code{method = "auto"} selects
Kruskal-Wallis for >2 groups and Wilcoxon rank-sum for 2 groups.
}
\examples{
# Two groups -> Wilcoxon by default under "auto".
set.seed(1)
df <- data.frame(group = rep(c("a","b"), each = 10),
                  x     = c(rnorm(10), rnorm(10, mean = 1)))
print(ctdna_run_group_test(df, "x", "group"))

# Force a t-test:
print(ctdna_run_group_test(df, "x", "group", method = "t"))

# Three groups -> Kruskal-Wallis by default.
df3 <- data.frame(group = rep(c("a","b","c"), each = 10),
                   x = c(rnorm(10), rnorm(10, mean = 1),
                         rnorm(10, mean = 2)))
print(ctdna_run_group_test(df3, "x", "group"))
}
\seealso{
\code{\link{ctdna_pairwise_ranksum}} (all pairwise
  comparisons); \code{\link{ctdna_pval_label}} (format the returned
  p for display).
}
