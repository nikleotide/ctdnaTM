% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict.R
\name{dnaseq_dict_add}
\alias{dnaseq_dict_add}
\title{Add a vendor-specific alias to the dnaseq dictionary}
\usage{
dnaseq_dict_add(sub, canonical, aliases)
}
\arguments{
\item{sub}{One of \code{"variant_type"}, \code{"molecular_consequence"},
\code{"cnv_type"}.}

\item{canonical}{The canonical entry name (must already exist under
\code{sub}). See \code{\link{dnaseq_dict}} for the full canonical
universe.}

\item{aliases}{Character vector of raw vendor strings to add.}
}
\value{
The updated dictionary, invisibly.
}
\description{
Convenience wrapper for
\code{dnaseq_dict("sub.entry" = aliases)}. Appends one or more raw
vendor strings to an existing canonical entry so downstream
normalisation (inside \code{\link{ctdna_prepare}} /
\code{\link{ctdna_prep_add}}) recognises those strings.
Duplicates are removed case-insensitively, so it is safe to call
multiple times with overlapping alias sets.
}
\examples{
# Restore defaults so this example is idempotent.
dnaseq_dict_reset()

# Register a vendor-specific string for the "missense" canonical entry.
dnaseq_dict_add("molecular_consequence", "missense", "MyVendor_Missense")

# Verify: the alias now resolves to "missense".
print(dnaseq_dict_lookup("molecular_consequence", "MyVendor_Missense"))

# Clean up.
dnaseq_dict_reset()
}
\seealso{
\code{\link{dnaseq_dict}} (get / set / list the whole dict);
  \code{\link{dnaseq_dict_reset}} (restore shipped defaults);
  \code{\link{dnaseq_dict_lookup}} (resolve raw values to canonical
  names).
}
