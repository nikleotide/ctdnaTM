% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{ctdna_plot_pct_change_by_dose_time}
\alias{ctdna_plot_pct_change_by_dose_time}
\title{Percent-change boxplots by dose and time, grouped by RECIST}
\usage{
ctdna_plot_pct_change_by_dose_time(
  df,
  time_col = .o("time"),
  dose_col = .o("dose"),
  recist_col = .o("recist"),
  value_col = "Ctdna_percentage_change",
  recist_grouping = c("three", "two", "four", "four_alt", "five", "six"),
  show_points = TRUE,
  show_stats = FALSE,
  y_scale = "linear",
  y_clip = c(-100, 200),
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  xlab = NULL,
  ylab = NULL,
  point_size = .o("point_size"),
  point_alpha = .o("point_alpha"),
  xlim = NULL,
  ylim = NULL,
  legend_position = .o("legend_position"),
  indication = NULL,
  indications = NULL
)
}
\arguments{
\item{df}{Long data frame with required columns (see arguments).}

\item{time_col}{Column for the time axis. Default \code{"time_point"}.}

\item{dose_col}{Column for dose (x within each panel). Default
\code{ctdna_opts("dose")}.}

\item{recist_col}{Column for RECIST values. Default
\code{ctdna_opts("recist")}.}

\item{value_col}{Column for the y-axis (percent change). Default
\code{"Ctdna_percentage_change"}.}

\item{recist_grouping}{One of \code{"three"} (default), \code{"two"}
(R/NR), \code{"four"}, \code{"four_alt"}, \code{"five"},
\code{"six"}; see \code{\link{ctdna_stratify_recist}}.}

\item{show_points}{Logical. Overlay individual subject points jittered
on top of each box. Default \code{TRUE}.}

\item{show_stats}{Logical. Annotate per-cell p-values (Kruskal-Wallis
across doses within each cell). Default \code{FALSE} (opt-in).}

\item{y_scale}{One of \code{"linear"} (default), \code{"log10"},
\code{"log2"}, \code{"log"}, or \code{"log_<N>"}.}

\item{y_clip}{Numeric \code{c(lo, hi)} used to bound the y-axis
view when the data has extreme outliers (a single
\eqn{+10{,}000\%} row would otherwise dominate the scale).
Default \code{c(-100, 200)}. Pass \code{NULL} to render the
full unclipped range. Overridden by an explicit \code{ylim}.}

\item{title, subtitle, caption, xlab, ylab}{Plot labels (auto-derived if NULL).}

\item{point_size, point_alpha}{Display for jittered points.}

\item{xlim, ylim}{Optional numeric \code{c(lo, hi)} axis limits.
\code{NULL} (default) uses the data range for \code{xlim} and
\code{y_clip} for \code{ylim}.}

\item{legend_position}{One of \code{"right"} (default), \code{"left"},
\code{"top"}, \code{"bottom"}, \code{"none"}.}

\item{indication}{Optional indication (cohort) restriction. A single string
or a character vector of Indication values, e.g.
\code{indication = "HNSCC"} or \code{indication = c("HNSCC","ES_SCLC")}.
\code{NULL} (the default) uses every indication present in the data.
Values are matched against the canonical \code{Indication} column,
resolved via \code{ctdna_opts("indication")} with fallbacks to
\code{Indication}, \code{indication}, \code{Cancertype} and
\code{Cancer_Type}. This is the modern spelling of the historical
\code{cancer_type=} argument.}

\item{indications}{Optional character vector of Indication values (e.g.
\code{c("NSCLC","mCRPC")}). When set, restricts the analysis to
patients whose Indication is in this set. Lookup falls through
Indication -> indication -> Cancertype -> Cancer_Type. \code{NULL}
(default) = no filter.}
}
\value{
A ggplot object.
}
\description{
Wrapped boxplots showing percent change in tumor burden (default
column \code{Ctdna_percentage_change}) stratified by dose (x within
each panel), time point (panel columns), and RECIST grouping (panel
rows). Empty time x RECIST cells are kept as empty rectangles via
\code{drop = FALSE} so the grid stays rectangular.
}
\examples{
sim  <- ctdna_make_mock_study(n_patients = 20, seed = 1)
# Passing tf_change to ctdna_prepare() populates the required
# Ctdna_percentage_change column on prep$samples.
prep <- ctdna_prepare(
  infinity_report = sim$infinity_report,
  tf_change       = sim$tf_change,
  adam            = list(adsl = sim$clinical),
  verbose         = FALSE)
prep <- ctdna_sample_qc(prep, verbose = FALSE)

# Percent change by dose, faceted by visit x RECIST scheme.
p <- ctdna_plot_pct_change_by_dose_time(prep$samples,
  recist_grouping = "three")
print(p)
}
