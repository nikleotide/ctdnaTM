% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctdnaTM.R
\name{ctdna_run_pipeline}
\alias{ctdna_run_pipeline}
\title{Run the full ctdnaTM plotting pipeline (deprecated wrapper)}
\usage{
ctdna_run_pipeline(
  ctdna_df,
  expr_df = NULL,
  tmb_df = NULL,
  ihc_df = NULL,
  g74_df = NULL,
  g500_df = NULL,
  sim = NULL,
  stat_position = NULL,
  legend_position = .o("legend_position"),
  scheme = "three",
  plot_overrides = list(),
  compose = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{ctdna_df, expr_df, tmb_df, ihc_df, g74_df, g500_df, sim}{Legacy args.
Assembled into a temporary \code{prep}-style list and passed to
\code{ctdna_pipeline}.}

\item{stat_position}{Unused since v0.42.0 (kept for back-compat).}

\item{legend_position}{Default legend position, passed to plots
that accept it.}

\item{scheme, plot_overrides, compose, verbose}{Passed through to
\code{ctdna_pipeline}; \code{plot_overrides} is renamed to
\code{overrides}.}
}
\value{
Same shape as \code{ctdna_pipeline} output (a
  \code{ctdna_pipeline_result}).
}
\description{
Deprecated in v0.42.2 — call \code{\link{ctdna_pipeline}} directly.
This wrapper exists only so existing user code that calls
\code{ctdna_run_pipeline()} keeps working. It silently translates
the old positional layout into the new
\code{ctdna_pipeline(prep, ...)} contract.
}
