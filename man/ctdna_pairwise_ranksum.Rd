% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_pairwise_ranksum}
\alias{ctdna_pairwise_ranksum}
\title{All-pairs comparison test
Pairwise rank-sum comparison across >2 groups}
\usage{
ctdna_pairwise_ranksum(data, value, group, method = "wilcox")
}
\arguments{
\item{data, value, group}{See [ctdna_run_group_test()].}

\item{method}{\code{"wilcox"} (default) or \code{"t"}.}
}
\value{
A data frame with columns \code{group1}, \code{group2},
  \code{p}, \code{n1}, \code{n2}. \code{NULL} if fewer than two
  distinct non-\code{NA} groups are present.
}
\description{
Runs Wilcoxon rank-sum (default) or Welch's t-test on every
unordered pair of groups in \code{data[[group]]}. Returns one row
per pair with the p-value and both group sizes. No multiple-testing
correction is applied on the raw output; adjust downstream with
\code{stats::p.adjust()} if needed.
}
\examples{
set.seed(1)
df <- data.frame(
  group = rep(c("a","b","c"), each = 8),
  x     = c(rnorm(8), rnorm(8, mean = 0.8), rnorm(8, mean = 1.5)))
print(ctdna_pairwise_ranksum(df, "x", "group"))

# Welch's t on the same pairs:
print(ctdna_pairwise_ranksum(df, "x", "group", method = "t"))
}
\seealso{
\code{\link{ctdna_run_group_test}} (single test);
  \code{\link{ctdna_pval_label}} (format p-values).
}
