% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_plots.R
\name{alt_combine}
\alias{alt_combine}
\alias{alt_any}
\alias{alt_all}
\alias{alt_any_n}
\title{Combine-mode wrappers for `ctdna_alteration_grid` gene sets}
\usage{
alt_any(x)

alt_all(x)

alt_any_n(x, n)
}
\arguments{
\item{x}{Character vector of gene symbols.}

\item{n}{Integer >= 1 and <= length(x). For `alt_any_n` only.}
}
\value{
A character vector with S3 class `"ctdna_geneset"` and combine-mode
  metadata attached.
}
\description{
Wrap a gene vector inside a `gene_sets` list entry to specify how a patient
is scored as "altered" for that set: `alt_any(x)` = at least one gene in
`x` altered; `alt_all(x)` = every gene altered; `alt_any_n(x, n)` = at
least `n` genes altered. A bare character vector is treated as `alt_any`.
}
\examples{
# gene_sets = list(TSG   = alt_any(TSG_gene_set),
#                  HRR   = alt_all(ctdna_gene_set("HRR14")),
#                  ABCx2 = alt_any_n(c("A","B","C"), 2))
}
