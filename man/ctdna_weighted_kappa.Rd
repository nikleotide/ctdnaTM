% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{ctdna_weighted_kappa}
\alias{ctdna_weighted_kappa}
\title{Weighted Cohen's kappa for ordinal agreement}
\usage{
ctdna_weighted_kappa(tab, weights = c("quadratic", "linear"))
}
\arguments{
\item{tab}{A contingency table (matching row/col ordinal
categories, in the intended order).}

\item{weights}{\code{"quadratic"} (default; distance squared) or
\code{"linear"} (distance).}
}
\value{
Weighted kappa in \code{[-1, 1]}, or \code{NA_real_} if the
  table has fewer than two matching categories, is empty, or the
  expected agreement is exactly 1.
}
\description{
Linear or quadratic weighted kappa on a square contingency table
whose row and column categories are the same ordinal set (ordered
the same way). Weights penalise disagreements between distant
categories more than adjacent-category disagreements. Quadratic
weighting is the RECIST convention.
}
\examples{
# Perfect ordinal agreement -> 1.
tab <- as.table(matrix(c(10, 0, 0, 0,
                         0, 12, 0, 0,
                         0, 0, 8, 0,
                         0, 0, 0, 6), nrow = 4, byrow = TRUE,
                       dimnames = list(
                         rater1 = c("CR","PR","SD","PD"),
                         rater2 = c("CR","PR","SD","PD"))))
print(ctdna_weighted_kappa(tab))                  # 1
print(ctdna_weighted_kappa(tab, weights = "linear"))

# Adjacent-category disagreement is penalised less than far-apart
# disagreement:
tab2 <- as.table(matrix(c(8, 2, 0, 0,
                           2, 8, 2, 0,
                           0, 2, 6, 0,
                           0, 0, 0, 6), nrow = 4, byrow = TRUE,
                         dimnames = dimnames(tab)))
print(ctdna_weighted_kappa(tab2))
}
\seealso{
\code{\link{ctdna_cohen_kappa}} (unweighted / nominal);
  \code{\link{ctdna_concordance_test}}.
}
