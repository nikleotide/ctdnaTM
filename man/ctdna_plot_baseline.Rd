% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_baseline.R
\name{ctdna_plot_baseline}
\alias{ctdna_plot_baseline}
\title{Baseline characteristics across one or two grouping factors}
\usage{
ctdna_plot_baseline(
  prep,
  group_by = .o("dose"),
  subgroup_by = NULL,
  compare_by = c("group", "subgroup", "subgroup_within_group", "group_within_subgroup"),
  pairwise = TRUE,
  p_adjust = FALSE,
  measure = c("all", "methyl", "maxvaf", "meanvaf", "custom"),
  specific_columns = NULL,
  value_col = NULL,
  scheme = c("four", "three", "two", "four_alt", "five", "six"),
  scales = c("free_y", "fixed", "free_x", "free"),
  stat = c("auto", "kruskal", "wilcox", "anova", "t", "none"),
  show_stats = .o("show_stats"),
  show_n = .o("show_n"),
  drop_na = .o("drop_na"),
  title = "Baseline characteristics",
  subtitle = NULL,
  caption = NULL,
  xlab = NULL,
  ylab = NULL,
  point_size = .o("point_size"),
  point_alpha = .o("point_alpha"),
  stat_position = .o("stat_position"),
  legend_position = .o("legend_position"),
  facet = NULL,
  wrap = NULL,
  y_scale = "log10",
  ylim = NULL,
  xlim = NULL,
  baseline_visit = "auto",
  indication = NULL,
  indications = NULL
)
}
\arguments{
\item{prep}{A \code{ctdna_prep} object (\code{prep$samples} is
used) or a per-sample data frame at the sample-visit grain.}

\item{group_by}{Primary split, drawn on the x-axis. An
\code{ctdna_opts()} key (e.g. \code{"dose"}) or a literal column
name (\code{"Dose"}, \code{"RECIST"}, ...). Default
\code{ctdna_opts("dose")}.}

\item{subgroup_by}{Optional second split, drawn as fill colour
(e.g. \code{"RECIST"}). \code{NULL} (default) plots a single-
factor overview.}

\item{compare_by}{Statistical comparison strategy. One of
\code{"group"} (compare \code{group_by} levels; the only mode
that draws x-aligned brackets on the plot),
\code{"subgroup"} (compare \code{subgroup_by} levels; p-values
reported in the caption), \code{"subgroup_within_group"}
(compare subgroups within each group), or
\code{"group_within_subgroup"} (compare groups within each
subgroup). Requires \code{subgroup_by} for anything other than
\code{"group"}.}

\item{pairwise}{Logical. When \code{TRUE} (default) and there are
more than two levels, run pairwise post-hoc tests in addition
to the omnibus test. When \code{FALSE}, only the omnibus is
reported.}

\item{p_adjust}{Logical. Apply Holm p-value adjustment to
pairwise comparisons. Default \code{FALSE} (raw p-values).}

\item{measure}{Which TF panels to draw. \code{"all"} (default)
shows methylation TF, max VAF, and mean VAF side by side;
\code{"methyl"} / \code{"maxvaf"} / \code{"meanvaf"} pick a
single panel; \code{"custom"} draws whichever column
\code{value_col} names.}

\item{specific_columns}{Optional character vector of explicit
column names to plot as panels. Overrides \code{measure} --
useful for non-TF characteristics such as
\code{c("cfDNA_ng", "n.somatic", "TMB_score")}.}

\item{value_col}{Column name used when \code{measure = "custom"}.
Ignored otherwise.}

\item{scheme}{RECIST stratification scheme applied whenever a
RECIST column is used as \code{group_by} or \code{subgroup_by}.
One of \code{"four"} (default), \code{"three"}, \code{"two"},
\code{"four_alt"}, \code{"five"}, \code{"six"}. See
\code{\link{ctdna_stratify_recist}} for level definitions.}

\item{scales}{Facet-scale argument passed to
\code{ggplot2::facet_wrap}: \code{"free_y"} (default),
\code{"fixed"}, \code{"free_x"}, or \code{"free"}.}

\item{stat}{Statistical test used to compute p-values. \code{"auto"}
(default) picks Kruskal-Wallis for >2 groups and Wilcoxon for 2;
explicit choices are \code{"kruskal"}, \code{"wilcox"},
\code{"anova"}, \code{"t"}, or \code{"none"} (suppress tests
entirely).}

\item{show_stats}{Logical. When \code{TRUE} (default from
\code{ctdna_opts("show_stats")}), render p-value annotations
(brackets or caption). When \code{FALSE}, statistics are
computed but not drawn.}

\item{show_n}{Logical. When \code{TRUE} (default from
\code{ctdna_opts("show_n")}), print per-group n-count labels
below each x-axis category.}

\item{drop_na}{Logical. When \code{TRUE} (default from
\code{ctdna_opts("drop_na")}), silently drop rows with an
\code{NA} value on the grouping factor rather than showing
them as a "\code{NA}" x-axis category.}

\item{title}{Plot title. Default \code{"Baseline characteristics"}.
Pass \code{NULL} to suppress.}

\item{subtitle}{Plot subtitle. Default \code{NULL}.}

\item{caption}{Plot caption. Default \code{NULL}. The function may
append statistical annotations (e.g. omnibus p-values) to any
caption you supply.}

\item{xlab, ylab}{Axis labels. Default \code{NULL} = auto-derived
from the grouping factor / metric.}

\item{point_size, point_alpha}{Overlaid-point size / alpha, both
defaulting to \code{ctdna_opts()} values.}

\item{stat_position}{Position for statistical annotations, passed
through to the stat-bracket layer. Default from
\code{ctdna_opts("stat_position")}.}

\item{legend_position}{ggplot legend position:
\code{"right"} (typical default from
\code{ctdna_opts("legend_position")}), \code{"bottom"},
\code{"top"}, \code{"left"}, or \code{"none"}.}

\item{facet}{Alias for \code{wrap}; both names are accepted on every
function that supports panelling. Names a column to split the output
into separate panels. If both are supplied and they differ,
\code{facet} wins and a message is emitted.}

\item{wrap}{Alias for \code{facet}; both names are accepted on every
function that supports panelling. If both are supplied and they
differ, \code{facet} wins and a message is emitted.}

\item{y_scale}{Y-axis transform. \code{"log10"} (default),
\code{"linear"}, \code{"log2"}, or any transform name
\code{scales::trans_new} recognises.}

\item{ylim}{Optional numeric \code{c(lo, hi)} y-axis limit pair.
\code{NULL} = auto.}

\item{xlim}{Optional character vector fixing the x-axis level
order. \code{NULL} = data-driven order.}

\item{baseline_visit}{Which visit(s) count as baseline for
filtering. \code{"auto"} (default) uses the internal baseline
detector (matches \code{"Cycle 1 Day 1"}, \code{"C1D1"},
\code{"Baseline"}, \code{"Screening"} case-insensitively).
Pass a literal visit label or vector of labels to override;
pass \code{NULL} to skip baseline filtering entirely.}

\item{indication}{Optional indication (cohort) restriction. A single string
or a character vector of Indication values, e.g.
\code{indication = "HNSCC"} or \code{indication = c("HNSCC","ES_SCLC")}.
\code{NULL} (the default) uses every indication present in the data.
Values are matched against the canonical \code{Indication} column,
resolved via \code{ctdna_opts("indication")} with fallbacks to
\code{Indication}, \code{indication}, \code{Cancertype} and
\code{Cancer_Type}. This is the modern spelling of the historical
\code{cancer_type=} argument.}

\item{indications}{Optional character vector of Indication values
(e.g. \code{c("NSCLC", "mCRPC")}). When set, restricts the
analysis to patients whose Indication is in this set. Lookup
falls through \code{Indication -> indication -> Cancertype ->
Cancer_Type}. \code{NULL} (default) = no filter.}
}
\value{
A \code{ggplot} object.
}
\description{
Filters a prepared bundle to baseline samples (or a data frame at
baseline grain), then draws TF panels -- and optionally non-TF
characteristics like cfDNA input, somatic count, or TMB -- as
boxplots split by \code{group_by} on the x-axis and, optionally,
by \code{subgroup_by} drawn as fill colour. Per-panel statistics
are computed according to \code{compare_by} + \code{stat} and
overlaid as brackets or reported in the caption.
}
\examples{
sim  <- ctdna_make_mock_study(n_patients = 20, seed = 1)
prep <- ctdna_prepare(
  infinity_report = sim$infinity_report,
  adam            = list(adsl = sim$clinical),
  verbose         = FALSE)
prep <- ctdna_sample_qc(prep, verbose = FALSE)

# Baseline TF panels split by Dose (default group_by).
p1 <- ctdna_plot_baseline(prep)
print(p1)

# Two-factor split: Dose on x, RECIST as fill colour, methyl-TF only.
p2 <- ctdna_plot_baseline(prep,
  group_by    = "Dose",
  subgroup_by = "RECIST",
  compare_by  = "group",
  measure     = "methyl")
print(p2)

# Non-TF panels: plot cfDNA input and somatic count instead.
p3 <- ctdna_plot_baseline(prep,
  specific_columns = c("cfDNA_ng", "n.somatic"),
  y_scale          = "linear")
print(p3)
}
\seealso{
\code{\link{ctdna_plot_reduction}} (on-treatment
  reduction distribution; inherits most args from this function);
  \code{\link{ctdna_plot_longitudinal}} (time-course view);
  \code{\link{ctdna_boxplot}} (generic boxplot for any column);
  \code{\link{ctdna_stratify_recist}} (RECIST scheme
  definitions).
}
